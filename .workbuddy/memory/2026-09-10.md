# 2026-09-10

## UI 重设计「书房」（用户反馈页面丑 + 新手引导差 + 要考虑分辨率）
- 新增 `frontend/src/app/globals.css` 设计系统：宣纸/墨/朱砂色板、宋体衬线、纸纹、
  深浅色变量、zinc 色阶重映射（10 个面板自动换肤，约 3000 行没动）。
- 重写 `app/page.tsx` 首页：品牌 Hero（印章 + 稿纸 SVG 插画 `components/hero-sheet.tsx`）、
  四步新手引导（`components/onboarding.tsx`，1/2/5 列自适应 + localStorage 可收起）、
  书架封面墙（`components/novel-cover.tsx` 按书名确定性生成传统色竖排封面）。
- 重写 `app/workspace/[id]/page.tsx` 外壳：宽屏左侧分组导航 + 窄屏可横滑标签条；
  顶栏显示书名；新增 `FirstRunGuide`（首次进入的 4 步路径引导，按项目记忆已读状态）。
- `layout.tsx` 加 viewport themeColor；`writing-panel.tsx` 章节正文改 `.reading`（宋体大行距）。
- 分辨率适配：根字号 clamp、版心 clamp、引导 1/2/5 列、工作台 ≥1280 用侧栏。
- 已验证：414/900/1100/1366/1600/2560 宽度 + 深浅色，生产构建通过（tsc + next build）。

## 顺手修的 bug
- 前端 `Novel.description` → 后端实际是 `premise`，简介之前永远存不上/显示不出来。

## 踩坑记录
- headless Chrome `--screenshot` 等不到客户端 fetch 与入场动画 → 写了 `scripts/shot.mjs`（CDP）。
- 用 `127.0.0.1:3000` 访问 dev 会因 Next 源站保护 404 HMR 且不水合，`localhost:3000` 正常。
- 验证用的 4 本演示小说已从 SQLite 清除，数据库恢复原状（原本就是空的）。

## 二次改造「去营销化 → 工具应用」（用户反馈：像企业网站首页，不像 AI 写小说工具）
- 参考了两个开源项目：`D:/Downloads/arboris-novel-main`（Material3，入口即项目网格 + h-screen 应用壳）、
  `D:/Downloads/neuro-book-master`（VSCode 式 IDE：活动栏/工具面板/画布，nb-ui e2e 快照可看视觉）。
- 首页 `app/page.tsx` 重写为「项目工作台」：48px 紧凑顶栏（品牌+新建）、继续写作条、
  作品卡片网格 + 虚线新建卡（arboris 模式）、新建改弹窗 `CreateDialog`、
  空书架 = 欢迎 + 五步指引 + 内联表单一屏收完。Hero/页脚/锚点导航全部移除，`hero-sheet.tsx` 已删。
- 工作台 `app/workspace/[id]/page.tsx` 重写为 IDE 外壳：`h-dvh overflow-hidden` 整页不滚、
  主画布内部滚动；左侧 188px 工具面板（SVG 图标 + 创作/资料/质量分组 + 底部小抄）；
  顶栏 = 返回书架 + 书名 + 「本地运行」状态点；中窄屏仍是横滑标签条。
- 结论模式：工具类产品 = 应用壳（满视口、内部滚动、密集信息），营销页结构（Hero/eyebrow/页脚）都要去掉。
- 验证：tsc + next build 通过；1440/1600/1100/414 + 深浅色截图核对；演示数据已清空。
