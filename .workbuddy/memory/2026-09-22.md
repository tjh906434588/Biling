# 2026-09-22

## 小说创建流程 + 版本联动审查
- 走查了 backend/app + frontend/src 的蓝图/大纲/写作三页主线。
- 架构定心：三页互斥渲染、各自重挂载重拉库，跨页不会共享 React 状态；评价↔正文版本对齐（selectedVersion.id 绑定）这一历史高危点已修复。
- **真实 BUG**：writing-panel 快速切章竞态——`loadDetail(no)` 三个独立 await 无「丢弃旧响应」守卫，快速点 A→B 时慢的 A 响应覆盖 B 的 detail，导致左栏高亮 B、右栏显示 A 正文/评价；连带 `isStaleForActiveOutline` 误判。修复=reqNoRef/AbortController 守卫。
- **遗漏**：评价面板「旧版本评价已折叠」提示未实现（工作记忆描述的预期 UI 代码里没有）。
- **行为注意（非 bug）**：切生效蓝图不撤销旧蓝图批准的 outline；会隐藏旧蓝图导入设定（版本感知预期行为，但「只微调蓝图」易误判丢世界设定）。
- 交付：报告 `D:/Project/personal/Biling/项目结构梳理与版本联动审查.md` + 架构图。

## 快速切章竞态 BUG 已修复
- 在 `frontend/src/components/writing-panel.tsx` 加 `loadDetailReqRef = useRef(0)` 请求序号守卫。
- `loadDetail` 入口 `req = ++loadDetailReqRef.current`；`getChapter`、`listReviews` 返回后各加 `if (req !== loadDetailReqRef.current) return` 丢弃过期写入。
- `loadApprovedOutlines` 增加可选 `req` 参数，内部 await 后校验，避免慢响应改写新章 `approvedOutline`/`isStaleForActiveOutline`。
- 无 req 的挂载默认加载调用不受影响。报告 3.2 节状态已由 🔴 改为 ✅ 已修复。

## 遗漏项「旧版本评价已折叠」提示已补
- 后端 `list_reviews` 本就返回该章全部版本评价（带 version_no/version_source/is_current）。
- 前端 writing-panel 评价弹窗底部加折叠区：`foldedReviews = reviews.filter(r => r.chapter_version_id !== selectedVersion.id)`、`foldedCount`；`foldedCount>0` 显示「另有 N 条旧版本评价已折叠」可展开，复用只读 `ReviewCard`（非选中版天然不渲染优化按钮）。
- 切章 `loadDetail` 内 `setShowFoldedReviews(false)` 收起上一章折叠态。报告 3.3 节已由 🟡 改为 ✅ 已处理。
