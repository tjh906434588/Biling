# Biling 项目结构梳理 · 小说创建流程 · 版本联动审查

> 审查时间：2026-09-22
> 范围：backend/app（FastAPI）+ frontend/src（Next.js App Router）的小说创建主线（蓝图 / 大纲 / 写作）及三页"切换版本"联动

---

## 一、项目结构（主线模块）

**后端 `backend/app/`**
- `main.py` 入口；`db/models.py` 全部表；`db/session.py` 会话
- 路由 `api/`：`novels`（建小说+设定）、`blueprints`（蓝图列表/激活/导入）、`outlines`（大纲列表/批准/版本）、`chapters`（章节详情/选版本/评价列表）
- 服务 `services/`：`pipeline.py`（落库+蓝图注入+账本同步+文风同步）、`setting_checker.py`（设定漏检）
- 智能体 `agents/`：`blueprint_architect / outliner / novelist / reviser / critic / extractor / context.py`（拼装上下文）

**前端 `frontend/src/`**
- 编排：`app/workspace/[id]/page.tsx` —— 六角色导航 + 按 tab **条件渲染**三个面板
- 三页：`components/blueprint-panel.tsx`、`outline-panel.tsx`、`writing-panel.tsx`
- 客户端：`lib/api.ts`、`lib/blueprint-run.ts`、`lib/ai-status.tsx`

**关键数据模型（版本语义）**
- `Blueprint`：版本链，`status` active/inactive（全书唯一 active）
- `Outline`：同章多版本，`status` draft/approved（同章最多一个 approved）
- `ChapterVersion`：`is_active` 布尔 + `source`(novelist_A/B/user_edit/merged) + `parent_version_id`（版本树）+ `outline_id`（精确关联所用大纲版本）
- `QualityReview`：挂在 `chapter_version_id` 上；`is_current` 由查询时 `v.is_active` 派生

---

## 二、小说创建流程（自洽，无断点）

```
建小说(title+premise)  →  工作台
  → 蓝图页：生成/导入 → 点「设为生效中」(activate，后台注入设定+文风)
  → 大纲页：逐章生成大纲 → 点「批准」(approve，后台注入角色+账本)
  → 写作页：生成章节正文 → 「定稿」(finalize=select_version) → 提取记忆层 → 评价 → 按评价优化
```
- 创建接口极简（`POST /api/novels` 仅写 title+premise），无副作用，状态机自洽。
- 各"生效"动作都**从请求生命周期解耦为后台任务**（`AgentTask`），刷新/切页不中断，前端轮询恢复——设计稳健。
- 设定/账本/角色都做了**版本感知过滤**：`list_settings` 按 active 蓝图、approved 大纲决定可见性。

---

## 三、版本联动审查（重点）

### 3.1 架构层面：已做对的事 ✅
- **三页互斥渲染**：`{tab==="write" && <WritingPanel/>}`，切页即卸载/重挂载，各自从 DB 重读。因此**任意一页的版本切换不会污染另一页的 React 状态**——这是避免跨页联动 BUG 的核心设计。
- **评价 ↔ 正文版本已对齐（历史 BUG 已修复）**：评价绑定 `selectedVersion.id`（UI 选中版）而非后端 `is_active`；`currentReview` 按选中版过滤；`handleRevise` 守卫 `review.chapter_version_id === selectedVersion.id`；`selectVersion` 正确移动 `is_active` 并同步章级正文/标题/大纲。切章/切版重拉库后一致，不会"拿旧版毛病改新正文"。
- **激活蓝图不覆盖手动文风**：`sync_active_blueprint_style` 只写 `style_directive`（蓝图识别文风），`style_directive_manual` 独立保留。

### 3.2 真实 BUG 🔴 → ✅ 已于 2026-09-22 修复
**写作页快速切章竞态（detail / activeNo / approvedOutline / reviews 错位）**
- 位置：`writing-panel.tsx` 章节点击 `onClick`（约 1553-1560）调用 `loadDetail(no)`；`loadDetail`（约 706-731）内部 `getChapter / loadApprovedOutlines / listReviews` 三个独立 await，**全程无"当前选中章仍是 no"的校验**。
- 复现：快速点章 A → 章 B。`loadDetail(A)` 较慢、`loadDetail(B)` 较快时，慢的 A 响应会 `setDetail(A)` 覆盖已设好的 B，导致：
  - 左栏高亮 B，右栏却显示 A 的正文/版本树/评价；
  - `approvedOutline`（由 `loadApprovedOutlines(no)` 写入）被 A 覆盖 → `isStaleForActiveOutline` 误判 → 提取/评价/修订门控错误。
- 严重度：中高（操作流程中"意料之外"的典型坑，但非数据损坏，刷新即恢复）。
- **修复方案（已落地）**：新增 `loadDetailReqRef = useRef(0)` 请求序号守卫。
  - `loadDetail` 入口 `const req = ++loadDetailReqRef.current`；
  - `getChapter` 返回后、以及 `listReviews` 返回后都 `if (req !== loadDetailReqRef.current) return` 丢弃过期写入；
  - `loadApprovedOutlines` 增加可选 `req` 参数，内部 `await listOutlines` 后同样校验，慢的旧响应不再改写新章的 `approvedOutline`；
  - 无 `req` 的调用（挂载时默认加载大纲）不受影响，正常生效。
  - 效果：快速切章时只有"最新一次请求"的结果会落库到 UI，左栏高亮章与右栏正文/评价/过期判断始终一致。

### 3.3 遗漏 / 与预期不符 🟡 → ✅ 已于 2026-09-22 处理
- **评价"旧版本评价已折叠"提示**：已落地。后端 `list_reviews` 本就返回该章**全部版本**的评价（带 `version_no / version_source / is_current`），前端据此在"评价与优化"弹窗底部加了折叠区：
  - 派生 `foldedReviews = reviews.filter(r => r.chapter_version_id !== selectedVersion.id)`、`foldedCount = foldedReviews.length`；
  - 当 `foldedCount > 0` 时，显示一行「▶ 另有 N 条旧版本评价（针对不同正文版本）已折叠，点击展开查看」；
  - 点击展开后，用只读 `ReviewCard` 列出这些历史评价（`ReviewCard` 对非选中版本天然不渲染"按评价优化"按钮、加"未选中"徽标，复用即安全）；
  - 切章时 `loadDetail` 内 `setShowFoldedReviews(false)` 收起上一章的折叠态。
  - 效果：用户现在能直观感知"当前章存在 N 条历史版本评价"，并可读历史详情，而非被静默丢弃。

### 3.4 行为注意（非 bug，但易误判）⚠️
- **切生效蓝图不会撤销旧蓝图批准的 outline**：激活蓝图 v2 后，v1 批准的 outline 仍 `approved` 并继续喂写作表单。用户可能以为"切蓝图=切世界"，实际旧大纲仍生效。若要严格"世界随蓝图切换"，需在激活蓝图时把同小说 outline 降回 draft（需谨慎，可能打断进行中的写作）。
- **切生效蓝图会隐藏旧蓝图导入的设定**：v1 的 blueprint-sourced 设定从设定库/上下文消失，`filter_settings_for_chapter` 不再包含；基于 v2 生成新章时看不到 v1 角色。这是版本感知的预期行为，但"只微调蓝图"的用户可能意外丢世界设定。
- **大纲批准不自动重写正文**：`approve_outline` 有 `has-chapter` 二次确认（正文基于其他版本不自动重写），写作页对旧版正文转只读（`isStaleForActiveOutline`），需手动重新生成——已用 toast 提示，流程闭环。

---

## 四、结论

- **创建流程完整、状态机自洽，无断点/遗漏。**
- **跨页面版本联动靠"重挂载重拉库"天然隔离，评价↔正文版本对齐这一历史高危点已修复，可放心。**
- **唯一需修的真实 BUG**：写作页快速切章的竞态（detail/activeNo 错位），建议加"丢弃旧响应"守卫。
- **可选补全**：评价面板"旧版本评价已折叠"提示；并确认"切蓝图是否应连带降 draft 旧大纲"的产品决策。
